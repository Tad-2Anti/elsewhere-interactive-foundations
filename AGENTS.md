# Project Instructions

Before changing the homepage experience, read `docs/ai-context.md` and `docs/implementation-plan.md`.

The approved interaction is the MIT-licensed Atmospheric Depth Gallery. Do not extend or polish the rejected circular ring lobby. Preserve upstream motion behavior during the initial TypeScript port, keep accessibility-critical content in HTML, and use only original or properly licensed media.

All meaningful design or architecture changes must update the corresponding document under `docs/` in the same change.
